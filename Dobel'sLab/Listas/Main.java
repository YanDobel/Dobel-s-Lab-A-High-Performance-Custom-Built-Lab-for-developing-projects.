package Listas;

public class Main {
    public static void main(String[] args) {

        /* record Aluno(String nome, int nota) {}

        LinkedList.LinkedList<Aluno> lista = new LinkedList.LinkedList<>();

        lista.InsertLast(new Aluno("Yan", 10));
        lista.InsertLast(new Aluno("Brad", 9));
        lista.InsertLast(new Aluno("Tarantino", 4));

        lista.find(aluno -> aluno.nota() == 10)
                .ifPresent(a -> System.out.println("maior nota: " + a.nome()));

        LinkedList.LinkedList<Aluno> aprovados = lista.filter(aluno -> aluno.nota() >= 7);

        System.out.println("Aprovados: " + aprovados);
        lista.map(Aluno::nome).forEach(System.out::println); */

        
    }
}
